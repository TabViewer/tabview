language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"

# command to install dependencies
install:
  - "pip install ."

# command to run tests
script: python -m unittest discover
branches:
    only:
        - master
notifications:
    email: false
