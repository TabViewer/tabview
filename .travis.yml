language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"

# command to install dependencies
install:
  - "pip install ."
  - "pip install -q flake8"

# command to run tests
script:
  - python -m unittest discover
  - flake8 --version
  - flake8 --ignore E501 .

branches:
    only:
        - master

notifications:
    email: false
